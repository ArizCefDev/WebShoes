@model IEnumerable<DTO.DTOEntity.ContactDTO>

<div class="map">
    @foreach (var item in Model)
   {
        <iframe width="100%" height="350" frameborder="0" scrolling="no" marginheight="0" marginwidth="0" src="@item.MapLocation"></iframe>

        <br>

        <small><a href="@item.MapLocation" style="color:#666;text-align:left;font-size:12px"></a></small>

    }
</div>
